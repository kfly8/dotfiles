#!/bin/bash
set -eu

if [ -z "${MEMOLIST_DIR:-}" ]; then
  echo "Error: MEMOLIST_DIR environment variable is not set"
  exit 1
fi

if [ $# -eq 0 ]; then
  echo "Enter title:"
  read TITLE
else
  TITLE=$1
fi

FILEPATH="${MEMOLIST_DIR}/${TITLE}.md"

nvim "$FILEPATH"
